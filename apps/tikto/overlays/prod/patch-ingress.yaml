apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tikto
spec:
  rules:
    - host: 18.136.110.57
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tikto
                port:
                  name: http

