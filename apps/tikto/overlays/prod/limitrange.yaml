apiVersion: v1
kind: LimitRange
metadata:
  name: tikto-prod-limits
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 250m
        memory: 256Mi
      default:
        cpu: "1"
        memory: 1Gi

