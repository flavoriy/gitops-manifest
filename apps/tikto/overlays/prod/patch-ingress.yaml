apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tikto
spec:
  rules:
    - host: tikto.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tikto
                port:
                  name: http

