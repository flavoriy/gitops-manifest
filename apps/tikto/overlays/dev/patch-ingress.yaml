apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tikto
spec:
  rules:
    - host: 18.139.31.55
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tikto
                port:
                  name: http

