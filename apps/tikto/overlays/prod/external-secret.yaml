apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tikto-secret
  namespace: tikto-prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: SecretStore
  target:
    name: tikto-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: tikto/secrets
